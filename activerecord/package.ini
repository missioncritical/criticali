version=1
package.version=0.2.10
package.summary="A PHP implmenetation of the ActiveRecord pattern for ORM"
package.description="Implements the ActiveRecod pattern for ORM.  This is a PHP implementation based on the version used in Ruby on Rails.  Like it's ruby counterpart, it ties classes and database tables together for business objects and reduces the need to write SQL statements for persistent storage."

[dependencies]
support=0.2.2

[config.defaults]
database/dsn="sqlite:#{dirname(__FILE__)}/../var/.db"

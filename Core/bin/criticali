#!/bin/sh

vulture_root="<?php echo $vulture_root ?>"
if [ ! -d "$vulture_root" ]
then
  vulture_root=`dirname $0`
  vulture_root="$vulture_root/../.."
fi

php="<?php echo $php_binary ?>"
if [ ! -x "$php" ]
then
  php="php"
fi

$php $vulture_root/Core/vulture_command.php $*
